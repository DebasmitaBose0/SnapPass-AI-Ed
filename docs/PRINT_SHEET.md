# Feature: Print-Ready Sheet Generation

This document tracks the tasks and specifications for print sheet layout generation.

## Tasks
- [ ] Implement grid placement algorithm to layout photos on standard paper sizes (A4, 4x6).
- [ ] Add optional cut lines and spacing margins around each photo.
- [ ] Create frontend print preview controls (copies count, paper size toggle).
- [ ] Implement `@media print` rules in `PrintPreviewPage.css` to clean up browser printing layout.
